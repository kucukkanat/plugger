# @plugger/react

React bindings — `PluggerProvider`, `PluggerSlot`, and hooks (`usePluginStore`, `useCommands`, `usePlugins`).

Part of [Plugger](../../README.md) — a browser-native meta framework for building
plugin systems for any SPA.

```bash
npm install @plugger/react @plugger/core
```

See the [documentation](../../README.md#documentation) for usage and live
playgrounds.

## License

MIT
